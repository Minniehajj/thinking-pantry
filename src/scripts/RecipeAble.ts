import { trpc } from "../utils/trpc";
import { recipeRouter } from "../server/router/recipe";

// function recipesAble (recipeData:  any[]): string[] {
//     return recipeData?.map((a) => {
//         const

//     });

// }

// export default recipesAble;
